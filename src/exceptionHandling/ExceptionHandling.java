 package exceptionHandling;

public class ExceptionHandling {
	
   // ExceptionHandling : handling the exceptions in java 
	
	// Error vs Exception
	
	 //Error : It's an issue occurs during runtime and it's not possible to handle it in the program. Example:StackOverflowError,OutOfMemoryError etc.
	 //Exception :It's an Issue occurs during the run time and it's possible to handle it in the program . Example :ArrayIndexOutOfBoundsException ,NullPointsException etc
	
	 // What is Exception Handling?
	 //We can control the exception in 2 ways.
	//1.When there is an exception, we can supress the exception and continue normal flow of the program.
	//2.When there is no exception ,We can create our own exception and fail the program.
	
	
	//Types of exception handling techniques in Java 
	//1.throws declaration==>When we know the exception details before itself (compulsorly we will get the exception).
	//2. try-catch block ==> When we don't know the exception details before itself (we may or may not get exception)
   //3. throw an custom exception ==> When there is no exception, we can create our own exceptions and fail the program.
	
		public static void main(String[] args) {
		
	}

}
